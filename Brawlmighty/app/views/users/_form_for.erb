<%= form_for(@character) do |f| %> <%= f.label :first_name %> <%= f.text_field :first_name %><br>
<%= f.label :last_name %>
 <%= f.text_field :last_name %><br>
<%= f.label :age %> <%= f.text_field :age %><br>
<%= f.collection_select(:doctor_id, Doctor.all, :id, :last_name, prompt: true) %><br>
 <%= f.submit %>
<% end %>
